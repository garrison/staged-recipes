# Build one package per MPI implementation. MPI is not optional for this
# package -- the extension links libmpi and the Python API takes an mpi4py
# communicator -- so there is no `nompi` variant.
mpi:
  - openmpi
  - mpich
